{
    "name": "notification-service",
    "version": "1.0.0",
    "description": "Service gửi thông báo email cho khách hàng",
    "main": "server.js",
    "scripts": {
      "start": "node server.js",
      "dev": "nodemon server.js"
    },
    "dependencies": {
      "express": "^4.18.2",        // Framework web cho Node.js
      "mysql2": "^3.6.0",         // Driver kết nối MySQL
      "nodemailer": "^6.9.4",     // Thư viện gửi email
      "dotenv": "^16.3.1",        // Đọc file .env
      "cors": "^2.8.5",           // Cho phép cross-origin requests
      "body-parser": "^1.20.2",   // Parse request body
      "joi": "^17.9.2"            // Validate dữ liệu đầu vào
    },
    "devDependencies": {
      "nodemon": "^3.0.1"         // Auto restart server khi code thay đổi
    }
  }
  